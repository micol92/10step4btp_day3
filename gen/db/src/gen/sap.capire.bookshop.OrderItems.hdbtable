-- generated by cds-compiler version 2.5.2
COLUMN TABLE sap_capire_bookshop_OrderItems (
  ID INTEGER NOT NULL,
  orderID INTEGER,
  bookID INTEGER,
  amount INTEGER,
  PRIMARY KEY(ID)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN sap_capire_bookshop_Orders AS parent ON (parent.ID = orderID),
  MANY TO ONE JOIN sap_capire_bookshop_Books AS book ON (book.ID = bookID)
)